# railway.toml
[build]
builder = "NIXPACKS"  # 🔧 يقول لـ Railway: "استخدم Nixpacks للبناء"

[deploy]
startCommand = "cd livraison-backend && npm start"  
# 🎯 يقول: "ادخل لمجلد livraison-backend ثم شغل npm start"

[env]
PORT = "8080"  # ⚙️ يحدد منفذ التشغيل